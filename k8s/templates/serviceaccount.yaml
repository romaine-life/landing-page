# Static-only pod; no Azure SDK calls. This ServiceAccount is kept as the
# stable Deployment identity used by the existing landing-page workload.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name | quote }}
  namespace: {{ .Release.Namespace }}
